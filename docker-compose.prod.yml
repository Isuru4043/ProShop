services:
  app:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: proshop_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
